const router = require("express").Router();

router.post("", () => {});
router.put("bars/:id", () => {});
router.delete(":id", () => {});
router.get("bars", () => {});
router.get("bars/:id", () => {});

module.exports = router;
